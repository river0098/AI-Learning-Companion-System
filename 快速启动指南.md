# AI陪伴学习系统 - 快速启动指南

**最后更新**: 2025-11-03
**版本**: 2.0.0

---

## 🚀 5分钟快速启动

这是最简单快捷的启动方式，只需要3个命令：

```bash
# 1. 安装依赖（首次运行需要）
pip3 install fastapi "uvicorn[standard]" pydantic

# 2. 进入项目目录
cd /path/to/AI-Learning-Companion-System/backend/src

# 3. 启动简化版服务器
python3 api_server_simple.py
```

**完成！** 打开浏览器访问: **http://localhost:8000**

---

## ✅ 系统已修复的问题

### 问题1: 网页打开后显示无样式 ✅ 已解决
**原因**: FastAPI未配置静态文件服务
**解决**: 添加了静态文件挂载配置

### 问题2: 缺少统一导航 ✅ 已解决
**原因**: 各功能页面独立，没有中心入口
**解决**: 创建了dashboard.html主控台页面

### 问题3: 登录跳转混乱 ✅ 已解决
**原因**: 登录后直接跳转到学习页面
**解决**: 修改为跳转到主控台

### 问题4: 依赖问题导致无法启动 ✅ 已解决
**原因**: 完整版服务器依赖过多（opencv、torch等）
**解决**: 创建了简化版服务器，只需最基本依赖

---

## 📋 完整启动步骤

### 步骤1: 安装Python（如果还没有）

**检查Python版本**:
```bash
python3 --version
```

需要Python 3.8或更高版本。

**如果没有Python**:
- Ubuntu/Debian: `sudo apt-get install python3 python3-pip`
- macOS: `brew install python3`
- Windows: 从 [python.org](https://python.org) 下载安装

---

### 步骤2: 安装依赖

**最小化依赖（推荐）**:
```bash
pip3 install fastapi "uvicorn[standard]" pydantic
```

这只需要约30MB，安装非常快。

**如果遇到权限问题**:
```bash
pip3 install --user fastapi "uvicorn[standard]" pydantic
```

**验证安装**:
```bash
python3 -c "import fastapi; print('✅ FastAPI已安装')"
```

---

### 步骤3: 启动服务器

**方式1: 使用简化版服务器（推荐）**

```bash
cd AI-Learning-Companion-System/backend/src
python3 api_server_simple.py
```

**方式2: 使用启动脚本**

```bash
cd AI-Learning-Companion-System
bash start_simple.sh
```

---

### 步骤4: 访问网页

服务器启动后，会显示:

```
============================================================
🚀 AI陪伴学习系统 简化版服务器启动
============================================================
📡 访问地址: http://localhost:8000
📖 API文档: http://localhost:8000/docs
⚠️  这是简化版服务器，仅用于测试网页加载
💡 测试账号:
   邮箱: test@example.com
   密码: password123
   手机: 13800138000
   验证码: 123456
============================================================
```

**打开浏览器**:
- 主地址: http://localhost:8000
- API文档: http://localhost:8000/docs

---

## 🎯 测试清单

### 测试1: 登录功能

1. 打开 http://localhost:8000
2. **检查**: 页面样式完整（渐变背景、玻璃态卡片、动画效果）
3. 选择"手机号"标签
4. 输入手机号: `13800138000`
5. 点击"获取验证码"
6. 输入验证码: `123456`
7. 点击"登录"
8. **预期**: 显示"登录成功！正在跳转..."，然后跳转到主控台

### 测试2: 主控台功能

1. 登录成功后应自动跳转到 http://localhost:8000/dashboard
2. **检查项**:
   - ✅ 顶部导航栏显示正确
   - ✅ 欢迎语显示"欢迎回来"
   - ✅ 快速操作按钮可见（开始学习、账号设置、学习报告）
   - ✅ 功能卡片网格显示6个卡片
   - ✅ 所有样式正确（渐变、阴影、动画）

### 测试3: 页面跳转

1. 点击顶部导航"学习中心" → 应跳转到 `/student`
2. 点击顶部导航"账号设置" → 应跳转到 `/account-settings`
3. 点击"开始学习"按钮 → 应跳转到 `/student`
4. **预期**: 所有跳转正常，页面样式完整

### 测试4: CSS加载

1. 打开浏览器开发者工具（F12）
2. 切换到Network标签
3. 刷新页面
4. 查找 `design-system-enhanced.css` 请求
5. **检查**:
   - ✅ 状态码: 200 OK
   - ✅ Content-Type: text/css
   - ✅ 文件大小: 约16KB

### 测试5: 账号设置页面

1. 访问 http://localhost:8000/account-settings
2. **检查**:
   - ✅ 页面样式现代化
   - ✅ 安全分数圆环显示（应显示65分）
   - ✅ 账号卡片显示已绑定的手机号
   - ✅ 建议列表显示

---

## 🔧 故障排查

### 问题1: 提示"ModuleNotFoundError"

**错误信息**:
```
ModuleNotFoundError: No module named 'fastapi'
```

**解决方法**:
```bash
pip3 install fastapi "uvicorn[standard]" pydantic
```

---

### 问题2: 端口8000被占用

**错误信息**:
```
ERROR: [Errno 98] Address already in use
```

**解决方法**:

**方法1: 查找并停止占用进程**
```bash
# 查找占用端口8000的进程
lsof -i :8000

# 停止进程（替换PID为实际进程ID）
kill <PID>
```

**方法2: 使用其他端口**

编辑 `api_server_simple.py` 最后几行:
```python
uvicorn.run(
    "api_server_simple:app",
    host="0.0.0.0",
    port=8001,  # 改为其他端口
    reload=True,
    log_level="info"
)
```

然后访问 http://localhost:8001

---

### 问题3: 页面显示空白或无样式

**可能原因及解决**:

**原因1: CSS文件不存在**
```bash
# 检查文件是否存在
ls -la frontend/public/styles/design-system-enhanced.css

# 如果不存在，检查Git是否正确克隆
git status
```

**原因2: 静态文件路径错误**

确认你从 `backend/src` 目录启动服务器:
```bash
cd backend/src
python3 api_server_simple.py
```

**原因3: 浏览器缓存**

强制刷新页面:
- Chrome/Firefox: Ctrl+Shift+R (Windows/Linux) 或 Cmd+Shift+R (Mac)
- 或清空浏览器缓存

---

### 问题4: 无法访问localhost

**检查防火墙**:
```bash
# Ubuntu/Debian
sudo ufw allow 8000

# 或关闭防火墙（测试环境）
sudo ufw disable
```

**尝试使用127.0.0.1**:
```
http://127.0.0.1:8000
```

---

### 问题5: 登录后不跳转

**检查浏览器控制台**:
1. 打开F12开发者工具
2. 切换到Console标签
3. 查看是否有JavaScript错误

**检查localStorage**:
```javascript
// 在Console中运行
console.log(localStorage.getItem('token'));
```

如果token为null，说明登录失败。

---

## 📊 功能对比

### 简化版服务器 vs 完整版服务器

| 功能 | 简化版 | 完整版 |
|------|--------|--------|
| 静态页面访问 | ✅ | ✅ |
| 手机号登录 | ✅ (模拟) | ✅ |
| 邮箱登录 | ✅ (模拟) | ✅ |
| 账号绑定/解绑 | ✅ (模拟) | ✅ |
| 安全分数显示 | ✅ (模拟) | ✅ |
| AI视觉分析 | ❌ | ✅ |
| 学习会话追踪 | ❌ | ✅ |
| 知识图谱 | ❌ | ✅ |
| 豆包大模型 | ❌ | ✅ |
| 数据持久化 | ❌ | ✅ |
| Redis集成 | ❌ | ✅ |

**简化版优点**:
- 安装快速（只需3个包）
- 启动迅速
- 无需数据库
- 适合测试前端页面

**完整版优点**:
- 完整功能
- 数据持久化
- 真实的AI分析
- 生产环境使用

---

## 🔄 从简化版切换到完整版

### 步骤1: 安装完整依赖

```bash
cd AI-Learning-Companion-System
pip3 install -r requirements.txt
```

注意：这可能需要较长时间（10-30分钟），特别是torch和opencv-python。

### 步骤2: 配置豆包API密钥

编辑 `backend/src/ai_companion/doubao_api.py` 第301行:
```python
DEFAULT_API_KEY = "你的真实API密钥"
```

### 步骤3: 初始化数据库

```bash
cd backend/src
python3 -c "from storage.database import init_db; from storage.auth_database import auth_db; init_db(); auth_db.init_tables()"
```

### 步骤4: 启动完整版服务器

```bash
cd backend/src
python3 api_server_auth.py
```

---

## 📚 相关文档

- `系统问题分析与修复总结.md` - 详细的问题分析和解决方案
- `豆包API调用问题分析与解决方案.md` - AI功能使用指南
- `UI优化总结.md` - UI/UX设计说明
- `多登录方式安全增强实现.md` - 安全功能文档

---

## 💡 常用命令

### 查看服务器日志
```bash
# 服务器会实时显示日志
# 查看访问日志、错误信息等
```

### 停止服务器
```bash
# 按 Ctrl+C
```

### 检查系统状态
```bash
curl http://localhost:8000/api/status
```

### 测试API
```bash
# 发送SMS验证码
curl -X POST http://localhost:8000/api/auth/sms/send \
  -H "Content-Type: application/json" \
  -d '{"phone_number": "13800138000", "purpose": "login"}'

# 手机号登录
curl -X POST http://localhost:8000/api/auth/login/phone \
  -H "Content-Type: application/json" \
  -d '{"phone_number": "13800138000", "code": "123456"}'
```

---

## 🎓 测试账号

### 手机号登录
- 手机号: `13800138000`
- 验证码: `123456` (任何时候都可用)

### 邮箱登录
- 邮箱: `test@example.com`
- 密码: `password123`

### 注意
- 这些是测试账号，数据不会持久化
- 重启服务器后需要重新登录
- 适合功能测试和演示

---

## ✨ 下一步

完成测试后，你可以：

1. **开发新功能** - 在简化版基础上添加功能
2. **切换到完整版** - 使用完整的AI分析功能
3. **自定义界面** - 修改CSS和HTML
4. **集成数据库** - 添加数据持久化
5. **部署到服务器** - 使用Nginx+Gunicorn部署

---

## 🙋 获取帮助

**如果遇到问题**:

1. 检查本文档的"故障排查"章节
2. 查看服务器日志输出
3. 检查浏览器控制台（F12 → Console）
4. 查看Network标签检查请求状态

**常见问题**:
- 页面无样式 → 检查CSS文件路径
- 无法登录 → 检查API请求是否成功
- 服务器启动失败 → 检查依赖是否安装完整

---

## 🎉 成功指标

如果看到以下现象，说明系统运行正常：

- ✅ 登录页面有渐变背景和玻璃态效果
- ✅ 可以使用手机号123456验证码登录
- ✅ 登录后跳转到主控台
- ✅ 主控台显示6个功能卡片
- ✅ 账号设置页面显示安全分数圆环
- ✅ 所有页面跳转正常
- ✅ CSS样式完整加载

**恭喜！系统已经可以正常使用了！** 🎊

---

**版本**: 2.0.0-simple
**最后更新**: 2025-11-03
**维护者**: AI Learning Companion Team

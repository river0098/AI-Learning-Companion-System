# 多登录方式集成指南

## 概述

本系统已成功集成多种登录方式，包括：
- ✅ **手机号 + 验证码登录** - 自动注册，快速便捷
- ✅ **邮箱 + 密码登录/注册** - 传统方式，支持独立注册
- ✅ **用户名 + 密码登录** - 兼容旧系统
- ✅ **微信H5登录** - 需要配置微信公众号
- ✅ **微信小程序登录** - 需要在小程序环境使用
- ✅ **账号绑定/解绑** - 一个账号可绑定多种登录方式

---

## API端点清单

### 1. 认证相关

#### 发送短信验证码
```http
POST /api/auth/sms/send
Content-Type: application/json

{
  "phone_number": "13800138000",
  "purpose": "login"  // login, register, bind
}
```

#### 手机号登录
```http
POST /api/auth/login/phone
Content-Type: application/json

{
  "phone_number": "13800138000",
  "code": "123456",
  "device_id": "optional_device_id"
}
```

#### 邮箱登录
```http
POST /api/auth/login/email
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123",
  "device_id": "optional_device_id"
}
```

#### 邮箱注册
```http
POST /api/auth/register/email
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123",
  "nickname": "用户昵称",  // 可选
  "device_id": "optional_device_id"
}
```

#### 微信H5登录
```http
POST /api/auth/login/wechat_h5
Content-Type: application/json

{
  "code": "oauth_code_from_wechat",
  "device_id": "optional_device_id"
}
```

#### 微信小程序登录
```http
POST /api/auth/login/wechat_mini
Content-Type: application/json

{
  "code": "mini_program_code",
  "device_id": "optional_device_id"
}
```

#### 刷新Token
```http
POST /api/auth/token/refresh
Content-Type: application/json

{
  "refresh_token": "your_refresh_token"
}
```

#### 用户名登录（兼容旧系统）
```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "password123"
}
```

#### 用户名注册（兼容旧系统）
```http
POST /api/auth/register
Content-Type: application/json

{
  "username": "testuser",
  "email": "user@example.com",
  "password": "password123",
  "full_name": "测试用户",
  "user_type": "student"
}
```

---

### 2. 账号绑定/解绑（需要认证）

#### 绑定手机号
```http
POST /api/auth/bind/phone
Authorization: Bearer <access_token>
Content-Type: application/json

{
  "phone_number": "13800138000",
  "code": "123456"
}
```

#### 绑定邮箱
```http
POST /api/auth/bind/email
Authorization: Bearer <access_token>
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

#### 解绑认证方式
```http
POST /api/auth/unbind
Authorization: Bearer <access_token>
Content-Type: application/json

{
  "auth_type": "phone"  // phone, email, wechat_h5, wechat_mini
}
```

#### 获取已绑定账号列表
```http
GET /api/auth/accounts
Authorization: Bearer <access_token>
```

响应示例：
```json
{
  "success": true,
  "data": {
    "accounts": [
      {
        "auth_type": "phone",
        "auth_identifier": "138****8000",
        "is_verified": true,
        "created_at": "2025-11-03T10:00:00"
      },
      {
        "auth_type": "email",
        "auth_identifier": "user@example.com",
        "is_verified": true,
        "created_at": "2025-11-03T10:05:00"
      }
    ]
  }
}
```

#### 获取当前用户信息
```http
GET /api/auth/me
Authorization: Bearer <access_token>
```

响应包含：
- 用户基本信息
- 会员状态
- 使用时长
- 已绑定账号列表

---

## 前端页面

### 1. 统一登录页面
- **URL**: `/` 或 `/login`
- **文件**: `frontend/public/login_unified.html`
- **功能**:
  - 4种登录方式切换（手机号、邮箱、用户名、微信）
  - 手机号验证码登录（首次自动注册）
  - 邮箱登录/注册切换
  - 用户名登录（兼容旧系统）
  - 微信登录（需配置）

### 2. 账号设置页面
- **URL**: `/account-settings`
- **文件**: `frontend/public/account_settings.html`
- **功能**:
  - 查看已绑定账号
  - 绑定新账号（手机号、邮箱、微信）
  - 解绑账号（需至少保留一个）
  - 实时状态更新

---

## 数据库架构

### 核心表

#### 1. users（主用户表）
```sql
- id (主键)
- uuid (全局唯一标识)
- nickname (昵称)
- avatar (头像)
- user_type (用户类型: student, parent, teacher)
- membership_type (会员类型: free, vip)
- membership_expires (VIP过期时间)
- is_active (是否激活)
- is_verified (是否已验证)
- created_at, updated_at, last_login
```

#### 2. auth_accounts（认证账号表）
```sql
- id (主键)
- user_id (外键 -> users.id)
- auth_type (认证类型: phone, email, wechat_h5, wechat_mini)
- auth_identifier (认证标识: 手机号、邮箱、openid等)
- credential_data (凭证数据，JSON格式)
- password_hash (密码哈希，仅邮箱登录)
- salt (密码盐)
- is_verified (是否已验证)
- verified_at (验证时间)
- created_at, updated_at
- 唯一索引: (auth_type, auth_identifier)
```

#### 3. refresh_tokens（刷新令牌表）
```sql
- id (主键)
- user_id (外键 -> users.id)
- token (令牌字符串)
- device_id (设备ID)
- device_name (设备名称)
- ip_address (IP地址)
- user_agent (用户代理)
- expires_at (过期时间)
- is_revoked (是否已撤销)
- revoked_at (撤销时间)
- created_at, last_used
```

#### 4. login_history（登录历史表）
```sql
- id (主键)
- user_id (外键 -> users.id)
- auth_type (登录方式)
- ip_address (IP地址)
- user_agent (用户代理)
- device_id (设备ID)
- location (地理位置)
- is_success (是否成功)
- failure_reason (失败原因)
- created_at
```

#### 5. sms_verifications（短信验证码表）
```sql
- id (主键)
- phone_number (手机号)
- code (验证码)
- purpose (用途: login, register, bind)
- is_used (是否已使用)
- used_at (使用时间)
- ip_address (IP地址)
- expires_at (过期时间)
- created_at
```

---

## Token系统

### Access Token（访问令牌）
- **有效期**: 1小时
- **用途**: API请求认证
- **格式**: JWT
- **包含信息**:
  - user_id: 用户ID
  - username: 用户名
  - user_type: 用户类型

### Refresh Token（刷新令牌）
- **有效期**: 30天
- **用途**: 刷新Access Token
- **格式**: 随机字符串（URL-safe base64）
- **存储**: 数据库中，支持撤销
- **绑定**: 设备ID、IP、User-Agent

---

## 使用流程

### 流程1: 新用户手机号登录
1. 用户输入手机号
2. 点击"发送验证码"
3. 后端发送验证码（当前为模拟，返回：123456）
4. 用户输入验证码
5. 点击"登录"
6. 后端验证验证码
7. 检查手机号是否已注册
8. 未注册则自动创建用户和auth_account
9. 返回access_token和refresh_token
10. 前端跳转到学生页面

### 流程2: 老用户绑定手机号
1. 用户已登录（有access_token）
2. 访问`/account-settings`
3. 点击"绑定手机号"
4. 输入手机号，发送验证码
5. 输入验证码，点击确认
6. 后端验证并创建新的auth_account记录
7. 绑定成功，页面刷新显示已绑定

### 流程3: Token刷新
1. 前端检测access_token即将过期
2. 使用refresh_token调用`/api/auth/token/refresh`
3. 后端验证refresh_token有效性
4. 生成新的access_token
5. 更新last_used时间
6. 返回新的access_token

---

## 安全机制

### 1. 密码安全
- SHA256 + 随机salt哈希
- Salt长度：32字节（64个十六进制字符）
- 每个用户独立salt

### 2. 验证码安全
- 6位数字验证码
- 5分钟有效期
- 单次使用后失效
- IP地址记录

### 3. Token安全
- Access Token短期有效（1小时）
- Refresh Token长期有效（30天）
- 设备绑定（device_id）
- 支持撤销所有token
- 登录历史追踪

### 4. 账号安全
- 解绑前检查是否有其他登录方式
- 登录失败记录
- 异常登录检测（未实现）

---

## 配置说明

### 微信H5登录配置
编辑 `backend/src/services/auth_service.py`:
```python
# 微信H5配置
WECHAT_H5_APPID = "your_h5_appid"
WECHAT_H5_SECRET = "your_h5_secret"
WECHAT_H5_REDIRECT_URI = "http://yourdomain.com/api/auth/wechat/callback"
```

### 微信小程序登录配置
```python
# 微信小程序配置
WECHAT_MINI_APPID = "your_mini_appid"
WECHAT_MINI_SECRET = "your_mini_secret"
```

### 短信服务配置
当前为模拟模式，验证码固定为：123456

生产环境需要集成真实短信服务（阿里云、腾讯云等）：
```python
def send_sms_code(phone_number: str, purpose: str = "login") -> Dict:
    code = auth_db.create_sms_code(phone_number, purpose)

    # TODO: 集成真实短信服务
    # sms_client.send(phone_number, f"验证码：{code}，5分钟内有效")

    return {"success": True, "code": code}  # 生产环境不要返回code
```

---

## 测试步骤

### 1. 启动后端
```bash
cd backend/src
python api_server_auth.py
```

### 2. 访问登录页面
浏览器打开: `http://localhost:8000/`

### 3. 测试手机号登录
- 输入任意手机号（如：13800138000）
- 点击"发送验证码"
- 输入：123456
- 点击"登录"
- 应该成功跳转到学生页面

### 4. 测试账号绑定
- 登录后访问: `http://localhost:8000/account-settings`
- 点击"绑定邮箱"
- 输入邮箱和密码
- 确认绑定
- 页面应显示已绑定

### 5. 测试邮箱登录
- 退出登录（清除localStorage）
- 回到登录页面
- 切换到"邮箱"标签
- 使用刚才绑定的邮箱和密码登录
- 应该成功登录

---

## 常见问题

### Q1: 短信验证码收不到？
A: 当前为模拟模式，所有验证码固定为 `123456`，生产环境需要集成真实短信服务。

### Q2: 微信登录无法使用？
A: 微信登录需要配置微信公众号或小程序的AppID和Secret，参考配置说明部分。

### Q3: 解绑账号失败？
A: 系统要求至少保留一种登录方式，如果只剩一个账号，则无法解绑。

### Q4: Token过期怎么办？
A: Access Token过期后，前端应使用Refresh Token调用 `/api/auth/token/refresh` 获取新的Access Token。

### Q5: 如何查看登录历史？
A: 登录历史存储在 `login_history` 表中，可以通过数据库查询或实现专门的API端点。

---

## 文件清单

### 后端文件
- `backend/src/storage/auth_database.py` - 认证数据库模型
- `backend/src/services/auth_service.py` - 认证服务逻辑
- `backend/src/api_server_auth.py` - API端点（已集成多登录方式）

### 前端文件
- `frontend/public/login_unified.html` - 统一登录页面
- `frontend/public/account_settings.html` - 账号设置页面
- `frontend/public/login.html` - 旧版登录页面（兼容）

### 文档文件
- `多登录方式系统说明.md` - 详细的系统设计文档
- `多登录方式集成指南.md` - 本文档

---

## 下一步优化建议

1. **集成真实短信服务**
   - 阿里云短信服务
   - 腾讯云短信服务
   - 添加发送频率限制

2. **完善微信登录**
   - 配置微信公众号
   - 配置微信小程序
   - 处理unionid合并账号

3. **添加更多登录方式**
   - Apple Sign In
   - Google OAuth
   - 游客模式

4. **增强安全性**
   - 添加CAPTCHA验证
   - 实现登录异常检测
   - IP地址封禁
   - 设备指纹识别

5. **迁移到Redis**
   - 验证码存储
   - Token黑名单
   - 频率限制计数器

6. **完善前端体验**
   - Token自动刷新
   - 断线重连
   - 更友好的错误提示
   - 加载动画

---

## 技术栈

- **后端**: Python 3.8+, FastAPI, SQLAlchemy, PyJWT
- **数据库**: SQLite（开发），推荐PostgreSQL/MySQL（生产）
- **前端**: 原生HTML/CSS/JavaScript
- **认证**: JWT + Refresh Token
- **加密**: SHA256 + Salt

---

**更新日期**: 2025-11-03
**版本**: 1.0.0

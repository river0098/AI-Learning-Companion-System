# AI陪伴学习系统 - 增强认证服务依赖

# 核心依赖
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
python-multipart>=0.0.6

# 数据库
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL (生产环境推荐)
# 或者
# pymysql>=1.1.0  # MySQL
# cryptography>=41.0.0  # PyMySQL需要

# Redis (必须)
redis>=5.0.0
hiredis>=2.2.0  # Redis性能优化

# JWT认证
PyJWT>=2.8.0
cryptography>=41.0.0

# HTTP请求
requests>=2.31.0
httpx>=0.25.0  # 异步HTTP客户端

# 数据验证
email-validator>=2.1.0
phonenumbers>=8.13.0  # 手机号验证

# 安全相关
passlib[bcrypt]>=1.7.4  # 密码哈希
python-jose[cryptography]>=3.3.0  # JWT
bcrypt>=4.1.0

# 短信服务SDK (根据选择的服务商)
# 阿里云短信
# aliyun-python-sdk-core>=2.13.36
# aliyun-python-sdk-dysmsapi>=1.0.0

# 腾讯云短信
# tencentcloud-sdk-python>=3.0.1000

# 工具库
python-dotenv>=1.0.0  # 环境变量
pytz>=2023.3  # 时区处理

# 监控和日志
prometheus-client>=0.19.0  # Prometheus监控

# 测试
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0  # 测试HTTP客户端

# 可选：CAPTCHA服务
# geetest>=3.2.0  # 极验
# python-recaptcha>=1.1.0  # Google reCAPTCHA

# 可选：地理位置
# geoip2>=4.7.0  # MaxMind GeoIP2
# ip2location>=8.10.0

# 可选：设备指纹
# user-agents>=2.2.0  # User-Agent解析
